# Astra — independent reporting-only review

**Disposition: no demonstrated blocker in this small patch. This is not overall approval.** No database defect or connection-retirement root cause is established by this review.

## Scope and witnessed checks

- First command verified the repository root exactly equals `/Users/monotykamary/VCS/working-remote/open-source/varve`.
- Read `AGENTS.md`, the diagnostic `CONTRACT.md`, `driver-scope.json`, and the scope verifier. Compared `benchmarks/timescale/benchmark.py` directly against `docs/evidence/frontier/v10/driver/benchmark.py`; reviewed changed `benchmarks/timescale/tests/test_runner.py` and `benchmarks/timescale/README.md`.
- Independently passed the four existing changed mixed/redaction tests using `/tmp/varve-timescale.DU0Znc/venv/bin/python -B`, from `benchmarks/timescale`: two mixed tests (0.022s), two redaction tests (0.005s). Disabled bytecode writes. Did not rerun the full 20-test suite; its success is supplied evidence, not independently witnessed here.
- Executed the verifier's read-only prefix, stopping before its log/receipt-writing section. Full normalized module AST comparison passed, as did exact baseline/current `core.py`, `Dockerfile`, and `requirements.txt` byte comparisons. Independently matched all seven current file hashes and both driver hashes against `driver-scope.json`. The receipt was not rewritten.
- No repository/source/test/private-control edits, network/database activity, agents, builds, installs, deploys, or commits. Only this review file was written.

## Acceptance ledger and source evidence

**H1 — nested failure leaf: witnessed pass for injected failures.** In `benchmark.py:993–1000`, a failed paired write escapes its inner TaskGroup; the outer TaskGroup is caught at `1047–1048`. At `1075–1078`, `MixedWorkloadError` now explicitly chains that original group. `redactor` (`100–124`) follows both group children and explicit causes. The real fake-client rejection test (`tests/test_runner.py:51–72`) asserts an ExceptionGroup cause and the injected leaf text; it exercises actual nested TaskGroups rather than only constructing an artificial group. The added cycle test (`104–118`) separately witnesses secret removal through a wrapper/group/leaf cycle.

**H2 — redaction/cycles/suppression/bounds: source-correct within the documented limits.** Identity tracking at `benchmark.py:104–112` prevents revisiting an exception. Causes take precedence over context; context is traversed only when not suppressed (`115–118`), matching Python chaining semantics. Credential replacement remains longest-first and happens before the 2,000-character slice (`101`, `119–122`), so clipping cannot expose a prefix of an otherwise exact matched secret. The new tests witness an actual cycle, suppressed versus visible context, and the 16-description limit (`tests/test_runner.py:104–130`). This is exact credential-value redaction, not a guarantee about transformed/escaped secrets or arbitrary sensitive exception text.

**H3 — failure-only samples: witnessed empty/nonempty read retention; generation retention inspected.** The four added keys at `benchmark.py:1067–1073` use the same raw arrays and summary expressions as the successful return (`1095–1101`). Reader arrays are appended together without an intervening await only after the paired read completes (`1012–1017`); generation is appended after encoding (`972`). TaskGroup exit precedes construction of the partial report, so these lists are no longer being appended by its workers. The rejection test witnesses empty read arrays with zero-sample summaries. The event-gated test (`tests/test_runner.py:132–168`) witnesses completed backend read values, computed summaries, one generation sample, one failed/ambiguous row and zero acknowledged rows. These are diagnostic completed samples, not a successful-run distribution. An individual backend read finishing while its paired read fails is not newly retained; the patch preserves the existing collection boundary.

**H4 — reporting-only isolation: witnessed pass.** Direct diff and independently replayed AST/byte assertions restrict the driver change to the redactor, four failure-report distributions, and explicit cause. Successful workload SQL, pacing, retries, caps, durability rules and acceptance behavior are unchanged. The verifier intentionally excludes the entire redactor from AST equality; its correctness was reviewed separately above. README lines 30–32 accurately separate failed-tail diagnostics from successful-run latency claims.

**Actual failure-to-report path: inspected, not end-to-end tested.** There is no `Report.fail` method in this driver. `run_fresh` catches `MixedWorkloadError` at `benchmark.py:1229–1235`, stores `error.partial`, updates manifest/oracles, flushes, and re-raises. Cleanup runs at `1258–1260`. `main` catches the escaping exception at `1460–1468`, redacts it, sets top-level `state = failed` and `failure`, flushes again, emits a failed final event and returns 1. `Report.flush` (`157–161`) serializes the same body through a temporary file and replacement. Thus normal cleanup preserves the cause and partial samples through the actual reporting path; successful final-state logic is bypassed.

**H5/H6 — not assessed.** Private formatter compatibility, effective remote receipts, runtime identity and scoped operational cleanup are outside this review. No deployment readiness or remote behavior conclusion follows from H1–H4.

## Nonblocking findings and recommendations

1. **Low — integration coverage gap.** The mixed tests stop at `MixedWorkloadError.partial` and call `redactor` directly (`tests/test_runner.py:61–72`, `156–168`). They do not witness `run_fresh` forwarding the partial body, `main` setting failed state/exit status, or persisted/emitted redaction. The existing persisted partial-query test (`170–191`) exercises a different query-suite path. Recommend a fake-only integration regression covering the mixed catch through terminal report serialization, including a secret-bearing nested leaf and retained samples. This is a coverage gap, not a demonstrated broken path.

2. **Low — some boundary assertions are weaker than their names suggest.** The `len(message) <= 2000` assertion at `tests/test_runner.py:118` uses a short message and would pass without the truncation code. The completed-read test checks pair summary consistency but does not assert pair raw length equals completed read count; it checks generation raw length but not its summary. Recommend an over-2,000-character case, explicit cause-versus-context precedence case, and pair-count/generation-summary assertions. Source inspection supports the current implementation despite these gaps.

## Limitations

The description/output caps intentionally can omit leaves in wide/deep groups or after long wrapper messages. They bound rendered output/unique descriptions, not arbitrary exception-string cost or group fan-out allocation (`benchmark.py:112–114`); group expansion and full-string construction predate this delta. Cleanup/flush failures can affect terminal reporting and were not injected. Tests use event coordination but still use real monotonic clocks/timeouts, not fully virtual clocks. This review does not reproduce the v10 failure, establish its cause, recover its missing historical samples, or support throughput, durability, distributed, production, or release claims.
