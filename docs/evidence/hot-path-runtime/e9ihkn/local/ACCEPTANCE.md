# Runtime and fresh Railway gate

Authorization: user requests all local runtime/fault checks, fixes as needed, then resumed Railway comparisons. Preserve prior dirty work and immutable archives. No GitHub comments or package publication.

- R1: real write/group/retry/reopen/fault behavior, exact receipts/floats and limits.
- R2: checkpoint/dependency publication, concurrent writes/GC, stale-root and failure protection.
- R3: actual DuckDB v2 resident hit/delta, catalog/empty changes, invalidation, limits and cancellation.
- R4: actual HTTP ingestion, query, shutdown/restart and durability smoke.
- R5: resolve failures without weakening assertions; targeted reruns, review, compiler/lint/format on final code.
- R6: bind final source and binary to passing evidence; freeze deploy inputs.
- R7: fresh Railway project/environment/service, deployment identity, resources and durability preflight; never reuse old controller activation authorizations.
- R8: repeat matched short Varve/Timescale comparisons on Railway; report throughput and latency with source/resource/workload identity. Escalate to sustained mixed load only on a valid pilot.

Completed 2026-09-17: R1–R6 passed on the source-bound qualification (268 selected Rust tests, 21 driver tests, two default-binary smokes, strict lint/format and independent reviews). R7 passed for the three exact owned Railway deployments with actual runtime identity/resource/durability checks. R8 executed three valid short trials and one valid sustained trial (1,450,000 acknowledged rows/backend in total, zero drops/ambiguity), plus 550k-row process-restart verification with independent time/value moments and retained aggregates. Benchmark compute is confirmed stopped, data/volumes retained, original app unchanged. The performance-frontier goal is NOT met overall. See varve/docs/HOT_PATH_RUNTIME.md and its hash-bound evidence bundle. A post-gate private test-only refinement passed separately; source equivalence is mechanically established. No S3, power-loss, saturation or production/HA claim. Local measurements are correctness evidence, not performance comparisons. Railway preparation may be read-only in parallel; no mutation/load before local gate passes. Stop unrelated/destructive changes and report permission or infrastructure blockers.
